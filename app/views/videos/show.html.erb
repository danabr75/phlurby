<p id="notice"><%= notice %></p>

<p>
  <strong>Filename:</strong>
  <%= @video.filename %>
</p>

<%= video_tag download_video_path(@video), type: @video.content_type, height: '300', width: '300', controls: true %>

<%= link_to 'Edit', edit_video_path(@video) %> |
<%= link_to 'Back', videos_path %>
